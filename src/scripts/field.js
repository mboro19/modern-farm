let fieldArray = [];

let addPlant = (seedObj) => {

    let seedlings = fieldArray.push(seedObj)

    return seedlings

};

export {addPlant};

let usePlants = () => {

    let plants = fieldArray;
    return plants

};

export {usePlants};

